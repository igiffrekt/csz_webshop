---
phase: 04-shopping-cart
plan: 04
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - apps/web/src/components/cart/CartIcon.tsx
  - apps/web/src/components/cart/CartSheet.tsx
  - apps/web/src/components/cart/CartItem.tsx
  - apps/web/src/components/cart/CartSummary.tsx
  - apps/web/src/components/layout/Header.tsx
  - apps/web/src/components/ui/scroll-area.tsx
  - apps/web/src/components/ui/separator.tsx
autonomous: true

must_haves:
  truths:
    - "User can view cart contents via slide-out panel"
    - "User can see item count badge on cart icon"
    - "User can update quantities in cart"
    - "User can remove items from cart"
    - "Cart shows subtotal correctly"
  artifacts:
    - path: "apps/web/src/components/cart/CartSheet.tsx"
      provides: "Slide-out cart panel"
      exports: ["CartSheet"]
      min_lines: 60
    - path: "apps/web/src/components/cart/CartItem.tsx"
      provides: "Cart item row with quantity controls"
      exports: ["CartItem"]
    - path: "apps/web/src/components/cart/CartIcon.tsx"
      provides: "Header cart icon with badge"
      exports: ["CartIcon"]
  key_links:
    - from: "apps/web/src/components/cart/CartSheet.tsx"
      to: "apps/web/src/stores/cart.ts"
      via: "useCartItems, useCartSubtotal"
      pattern: "useCart"
    - from: "apps/web/src/components/layout/Header.tsx"
      to: "apps/web/src/components/cart/CartIcon.tsx"
      via: "component import and render"
      pattern: "CartIcon"
---

<objective>
Create cart UI components for viewing and managing cart contents.

Purpose: Users need to view their cart, see what they've added, adjust quantities, remove items, and see the running total. The cart appears as a slide-out panel (Sheet) from the right side, triggered by clicking the cart icon in the header.

Output: CartIcon (header badge), CartSheet (slide-out panel), CartItem (quantity controls), CartSummary (totals display), all integrated into Header.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shopping-cart/04-RESEARCH.md
@.planning/phases/04-shopping-cart/04-02-SUMMARY.md
@apps/web/src/stores/cart.ts
@apps/web/src/components/layout/Header.tsx
@apps/web/src/components/ui/sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ScrollArea and Separator shadcn components</name>
  <files>
    apps/web/src/components/ui/scroll-area.tsx
    apps/web/src/components/ui/separator.tsx
  </files>
  <action>
Install ScrollArea and Separator components via shadcn CLI:

```bash
cd apps/web && pnpm dlx shadcn@latest add scroll-area separator
```

These are needed for the cart sheet layout - ScrollArea for scrollable item list, Separator for visual dividers.
  </action>
  <verify>
Verify scroll-area.tsx and separator.tsx exist in components/ui.
Run `pnpm build` to verify compilation.
  </verify>
  <done>
ScrollArea and Separator components available for cart UI.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CartIcon component with badge</name>
  <files>apps/web/src/components/cart/CartIcon.tsx</files>
  <action>
Create cart icon component that shows item count badge:

```typescript
'use client';

import { ShoppingCart } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { useCartItemCount } from '@/stores/cart';
import { useHydration } from '@/stores/useHydration';

interface CartIconProps {
  onClick?: () => void;
}

export function CartIcon({ onClick }: CartIconProps) {
  const itemCount = useCartItemCount();
  const hydrated = useHydration();

  return (
    <Button
      variant="ghost"
      size="icon"
      onClick={onClick}
      className="relative"
      aria-label={`Kosar${hydrated && itemCount > 0 ? ` (${itemCount} termek)` : ''}`}
    >
      <ShoppingCart className="h-5 w-5" />
      {hydrated && itemCount > 0 && (
        <span className="absolute -top-1 -right-1 h-5 w-5 rounded-full bg-primary text-primary-foreground text-xs flex items-center justify-center font-medium">
          {itemCount > 99 ? '99+' : itemCount}
        </span>
      )}
    </Button>
  );
}
```

Key points:
- Uses useHydration to prevent SSR mismatch (badge only shows after client hydration)
- Accessible aria-label with item count
- Badge shows 99+ if count exceeds 99
  </action>
  <verify>
Run `pnpm build` in apps/web to verify TypeScript compilation.
  </verify>
  <done>
CartIcon component shows cart with item count badge after hydration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create CartItem component with quantity controls</name>
  <files>apps/web/src/components/cart/CartItem.tsx</files>
  <action>
Create cart item row component following the pattern from 04-RESEARCH.md:

```typescript
'use client';

import Image from 'next/image';
import { Minus, Plus, Trash2 } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { useCartStore, type CartItem as CartItemType } from '@/stores/cart';
import { formatPrice } from '@/lib/formatters';
import type { CartItem as CartItemInterface } from '@csz/types';

interface CartItemProps {
  item: CartItemInterface;
}

export function CartItem({ item }: CartItemProps) {
  const updateQuantity = useCartStore((s) => s.updateQuantity);
  const removeItem = useCartStore((s) => s.removeItem);

  return (
    <div className="flex gap-4 py-4">
      {item.image ? (
        <div className="relative h-20 w-20 flex-shrink-0 rounded-md overflow-hidden bg-muted">
          <Image
            src={item.image}
            alt={item.name}
            fill
            className="object-cover"
            sizes="80px"
          />
        </div>
      ) : (
        <div className="h-20 w-20 flex-shrink-0 rounded-md bg-muted flex items-center justify-center">
          <span className="text-muted-foreground text-xs">Nincs kep</span>
        </div>
      )}

      <div className="flex-1 min-w-0">
        <h4 className="font-medium truncate">{item.name}</h4>
        {item.variantName && (
          <p className="text-sm text-muted-foreground">{item.variantName}</p>
        )}
        <p className="text-sm text-muted-foreground">SKU: {item.sku}</p>
        <p className="font-semibold mt-1">{formatPrice(item.price)}</p>
      </div>

      <div className="flex flex-col items-end justify-between">
        <Button
          variant="ghost"
          size="icon"
          className="h-8 w-8 text-muted-foreground hover:text-destructive"
          onClick={() => removeItem(item.id)}
          aria-label="Termek torlese"
        >
          <Trash2 className="h-4 w-4" />
        </Button>

        <div className="flex items-center gap-1">
          <Button
            variant="outline"
            size="icon"
            className="h-8 w-8"
            onClick={() => updateQuantity(item.id, item.quantity - 1)}
            disabled={item.quantity <= 1}
            aria-label="Mennyiseg csokkentese"
          >
            <Minus className="h-3 w-3" />
          </Button>

          <span className="w-8 text-center text-sm font-medium">
            {item.quantity}
          </span>

          <Button
            variant="outline"
            size="icon"
            className="h-8 w-8"
            onClick={() => updateQuantity(item.id, item.quantity + 1)}
            disabled={item.quantity >= item.maxStock}
            aria-label="Mennyiseg novelese"
          >
            <Plus className="h-3 w-3" />
          </Button>
        </div>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
Run `pnpm build` in apps/web to verify TypeScript compilation.
  </verify>
  <done>
CartItem component displays item with image, name, price, and quantity controls.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create CartSummary component</name>
  <files>apps/web/src/components/cart/CartSummary.tsx</files>
  <action>
Create cart summary component showing subtotal, discount, and total:

```typescript
'use client';

import { useCartSubtotal, useCartDiscount, useCartTotal, useCartCoupon } from '@/stores/cart';
import { formatPrice } from '@/lib/formatters';
import { Separator } from '@/components/ui/separator';

export function CartSummary() {
  const subtotal = useCartSubtotal();
  const discount = useCartDiscount();
  const total = useCartTotal();
  const coupon = useCartCoupon();

  return (
    <div className="space-y-3">
      <div className="flex justify-between text-sm">
        <span className="text-muted-foreground">Reszosszeg</span>
        <span>{formatPrice(subtotal)}</span>
      </div>

      {coupon && (
        <div className="flex justify-between text-sm text-green-600">
          <span>Kedvezmeny ({coupon.code})</span>
          <span>-{formatPrice(discount)}</span>
        </div>
      )}

      <Separator />

      <div className="flex justify-between font-semibold text-lg">
        <span>Osszesen</span>
        <span>{formatPrice(total)}</span>
      </div>

      <p className="text-xs text-muted-foreground">
        Az arak tartalmazzak a 27% AFAt
      </p>
    </div>
  );
}
```
  </action>
  <verify>
Run `pnpm build` in apps/web to verify TypeScript compilation.
  </verify>
  <done>
CartSummary component shows subtotal, coupon discount (if applied), and total.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create CartSheet slide-out panel</name>
  <files>apps/web/src/components/cart/CartSheet.tsx</files>
  <action>
Create the slide-out cart panel following the pattern from 04-RESEARCH.md:

```typescript
'use client';

import Link from 'next/link';
import {
  Sheet,
  SheetContent,
  SheetHeader,
  SheetTitle,
  SheetFooter,
} from '@/components/ui/sheet';
import { Button } from '@/components/ui/button';
import { ScrollArea } from '@/components/ui/scroll-area';
import { Separator } from '@/components/ui/separator';
import { ShoppingBag } from 'lucide-react';
import { useCartItems } from '@/stores/cart';
import { useHydration } from '@/stores/useHydration';
import { CartItem } from './CartItem';
import { CartSummary } from './CartSummary';

interface CartSheetProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function CartSheet({ open, onOpenChange }: CartSheetProps) {
  const items = useCartItems();
  const hydrated = useHydration();

  // Don't render content until hydrated to prevent mismatch
  if (!hydrated) {
    return (
      <Sheet open={open} onOpenChange={onOpenChange}>
        <SheetContent className="flex flex-col w-full sm:max-w-lg">
          <SheetHeader>
            <SheetTitle>Kosar</SheetTitle>
          </SheetHeader>
          <div className="flex-1 flex items-center justify-center">
            <p className="text-muted-foreground">Betoltes...</p>
          </div>
        </SheetContent>
      </Sheet>
    );
  }

  const itemCount = items.reduce((sum, item) => sum + item.quantity, 0);

  return (
    <Sheet open={open} onOpenChange={onOpenChange}>
      <SheetContent className="flex flex-col w-full sm:max-w-lg">
        <SheetHeader>
          <SheetTitle>
            Kosar {itemCount > 0 && `(${itemCount} termek)`}
          </SheetTitle>
        </SheetHeader>

        {items.length === 0 ? (
          <div className="flex-1 flex flex-col items-center justify-center gap-4">
            <ShoppingBag className="h-16 w-16 text-muted-foreground/50" />
            <p className="text-muted-foreground">A kosar ures</p>
            <Button variant="outline" onClick={() => onOpenChange(false)} asChild>
              <Link href="/termekek">Bongesszen termekeink kozott</Link>
            </Button>
          </div>
        ) : (
          <>
            <ScrollArea className="flex-1 -mx-6 px-6">
              <div className="divide-y">
                {items.map((item) => (
                  <CartItem key={item.id} item={item} />
                ))}
              </div>
            </ScrollArea>

            <div className="pt-4 space-y-4">
              <Separator />
              <CartSummary />
            </div>

            <SheetFooter className="mt-4 sm:flex-col sm:space-x-0 gap-2">
              <Button
                asChild
                size="lg"
                className="w-full"
                onClick={() => onOpenChange(false)}
              >
                <Link href="/penztar">Tovabb a penztarhoz</Link>
              </Button>
              <Button
                variant="outline"
                size="lg"
                className="w-full"
                onClick={() => onOpenChange(false)}
                asChild
              >
                <Link href="/termekek">Folytassa a vasarlast</Link>
              </Button>
            </SheetFooter>
          </>
        )}
      </SheetContent>
    </Sheet>
  );
}
```

Note: The checkout link (/penztar) is a placeholder that will be implemented in Phase 6.
  </action>
  <verify>
Run `pnpm build` in apps/web to verify TypeScript compilation.
  </verify>
  <done>
CartSheet component provides slide-out panel with item list, summary, and checkout link.
  </done>
</task>

<task type="auto">
  <name>Task 6: Integrate CartIcon and CartSheet into Header</name>
  <files>apps/web/src/components/layout/Header.tsx</files>
  <action>
Update Header component to include cart functionality.

The Header is currently a Server Component. We need to create a client component for the cart interaction.

Create `apps/web/src/components/layout/HeaderCart.tsx`:

```typescript
'use client';

import { useState } from 'react';
import { CartIcon } from '@/components/cart/CartIcon';
import { CartSheet } from '@/components/cart/CartSheet';

export function HeaderCart() {
  const [cartOpen, setCartOpen] = useState(false);

  return (
    <>
      <CartIcon onClick={() => setCartOpen(true)} />
      <CartSheet open={cartOpen} onOpenChange={setCartOpen} />
    </>
  );
}
```

Then update Header.tsx:
1. Import HeaderCart: `import { HeaderCart } from './HeaderCart';`
2. Find the existing cart icon placeholder (likely a ShoppingCart icon button)
3. Replace it with `<HeaderCart />`
4. Keep the mobile nav cart icon as-is for now (will be addressed if needed)

The Header should maintain its Server Component status where possible - only the cart interaction is client-side.
  </action>
  <verify>
Run `pnpm build` in apps/web.
Start dev server and verify:
1. Cart icon visible in header
2. Click cart icon - slide-out panel appears
3. Close button/overlay click closes panel
  </verify>
  <done>
Header displays cart icon with badge. Clicking opens CartSheet slide-out panel.
  </done>
</task>

</tasks>

<verification>
Full cart UI test:

1. Start all services and add items to cart (from Plan 03)

2. Test CartIcon:
   - Badge shows correct count
   - Count updates when items added
   - Badge hidden when cart empty

3. Test CartSheet:
   - Opens on cart icon click
   - Shows all cart items with images
   - Shows correct prices and subtotal

4. Test CartItem:
   - Plus button increases quantity (up to maxStock)
   - Minus button decreases quantity (stops at 1)
   - Trash button removes item
   - Quantity change updates subtotal immediately

5. Test empty state:
   - Clear cart via browser console: `useCartStore.getState().clearCart()`
   - Sheet shows empty state with link to products

6. Test production build:
   - `pnpm build` completes without errors
   - No hydration mismatch errors on page load
</verification>

<success_criteria>
- ScrollArea and Separator components added via shadcn
- CartIcon shows item count badge (0 hidden, 1-99 shown, 99+ capped)
- CartItem displays product info with quantity controls
- Quantity buttons respect min (1) and max (stock) limits
- Remove button deletes item from cart
- CartSummary shows subtotal, discount, and total
- CartSheet slides in from right on cart icon click
- Empty cart state shows friendly message and link to products
- All UI text in Hungarian
- No hydration mismatch errors
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-shopping-cart/04-04-SUMMARY.md`
</output>
