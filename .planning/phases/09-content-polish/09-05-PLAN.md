---
phase: 09-content-polish
plan: 05
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - apps/web/src/components/home/InstagramSection.tsx
  - apps/web/src/app/[locale]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Instagram section on home page"
    - "Placeholder images displayed"
    - "Link to Instagram profile"
  artifacts:
    - path: "apps/web/src/components/home/InstagramSection.tsx"
      provides: "Instagram section component"
      contains: "Instagram"
---

<objective>
Add Instagram placeholder section to home page.

Purpose: CONT-10 (Instagram placeholder section).

Output: Home page with Instagram feed placeholder.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create Instagram Section component</name>
  <files>
    apps/web/src/components/home/InstagramSection.tsx
  </files>
  <action>
Create Instagram section at `apps/web/src/components/home/InstagramSection.tsx`:

```typescript
import { Instagram } from 'lucide-react';
import Image from 'next/image';

// Placeholder images - in production, would fetch from Instagram API
const placeholderPosts = [
  { id: 1, alt: 'Tűzoltó készülék bemutató' },
  { id: 2, alt: 'Tűzvédelmi felszerelések' },
  { id: 3, alt: 'Szakmai tanácsadás' },
  { id: 4, alt: 'Termék karbantartás' },
  { id: 5, alt: 'Ügyfélszolgálat' },
  { id: 6, alt: 'Új termékek' },
];

export function InstagramSection() {
  const instagramUrl = 'https://instagram.com/csztuzvedelmi';

  return (
    <section className="py-16 bg-muted/50">
      <div className="container mx-auto px-4">
        <div className="text-center mb-8">
          <div className="flex items-center justify-center gap-2 mb-2">
            <Instagram className="h-6 w-6" />
            <h2 className="text-2xl font-bold">Kövessen minket Instagramon</h2>
          </div>
          <p className="text-muted-foreground">
            <a
              href={instagramUrl}
              target="_blank"
              rel="noopener noreferrer"
              className="hover:text-primary transition-colors"
            >
              @csztuzvedelmi
            </a>
          </p>
        </div>

        <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
          {placeholderPosts.map((post) => (
            <a
              key={post.id}
              href={instagramUrl}
              target="_blank"
              rel="noopener noreferrer"
              className="relative aspect-square bg-muted rounded-lg overflow-hidden group"
            >
              {/* Placeholder with gradient */}
              <div className="absolute inset-0 bg-gradient-to-br from-primary/20 to-primary/5" />

              {/* Placeholder icon */}
              <div className="absolute inset-0 flex items-center justify-center">
                <Instagram className="h-8 w-8 text-muted-foreground/50" />
              </div>

              {/* Hover overlay */}
              <div className="absolute inset-0 bg-black/50 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center">
                <Instagram className="h-8 w-8 text-white" />
              </div>
            </a>
          ))}
        </div>

        <div className="text-center mt-8">
          <a
            href={instagramUrl}
            target="_blank"
            rel="noopener noreferrer"
            className="inline-flex items-center gap-2 px-6 py-3 bg-primary text-primary-foreground rounded-lg hover:bg-primary/90 transition-colors"
          >
            <Instagram className="h-5 w-5" />
            Követés Instagramon
          </a>
        </div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
    - 6 placeholder tiles displayed
    - Link to Instagram profile
    - Hover effect on tiles
    - CTA button at bottom
  </verify>
  <done>Instagram section component created</done>
</task>

<task type="auto">
  <name>Task 2: Add Instagram section to home page</name>
  <files>
    apps/web/src/app/[locale]/page.tsx
  </files>
  <action>
Update home page to include Instagram section.

First, check if InstagramSection is exported from components/home/index.ts. If not, add export.

Add to `apps/web/src/components/home/index.ts` (if exists):

```typescript
export { InstagramSection } from './InstagramSection';
```

Then update `apps/web/src/app/[locale]/page.tsx` to import and render the section:

Add import at top:
```typescript
import { InstagramSection } from '@/components/home/InstagramSection';
```

Add section before closing `</main>` tag:
```tsx
{/* Instagram Section */}
<InstagramSection />
```
  </action>
  <verify>
    - Instagram section appears on home page
    - Section positioned after main content
    - Responsive grid layout
  </verify>
  <done>Instagram section added to home page</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Instagram section visible on home page
2. 6 placeholder tiles displayed
3. Instagram link works
4. Hover effects functional

Requirements completed:
- CONT-10: Instagram placeholder section on home page
</verification>

<success_criteria>
- Home page shows Instagram section
- Placeholder tiles with hover effect
- Follow button links to Instagram
- Responsive on mobile
</success_criteria>
