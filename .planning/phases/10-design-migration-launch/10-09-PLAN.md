---
phase: 10-design-migration-launch
plan: 09
type: implement
wave: 4
depends_on: [10-01, 10-05]
files_modified:
  - apps/web/src/app/[locale]/termekek/page.tsx
  - apps/web/src/app/[locale]/termekek/[slug]/page.tsx
  - apps/web/src/app/[locale]/kategoriak/page.tsx
autonomous: true

must_haves:
  truths:
    - "Product listing uses enhanced cards"
    - "Product detail page polished"
  artifacts: []
---

<objective>
Polish product listing and detail pages to match new design system.

Purpose: Ensure consistent design across product pages.

Output: Polished product pages with new design elements.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update product listing page</name>
  <files>
    apps/web/src/app/[locale]/termekek/page.tsx
  </files>
  <action>
Update product listing to use ProductCardEnhanced:

1. Import ProductCardEnhanced instead of ProductCard
2. Update grid classes for better spacing
3. Add section header styling
4. Improve filter sidebar styling with new colors

Key changes:
- Replace ProductCard with ProductCardEnhanced
- Add "Termékeink" header above grid
- Update pagination styling to match design
- Improve filter checkbox styling with primary color
  </action>
  <verify>
    - Enhanced cards used
    - Consistent styling
    - Filters work
  </verify>
  <done>Product listing updated</done>
</task>

<task type="auto">
  <name>Task 2: Polish product detail page</name>
  <files>
    apps/web/src/app/[locale]/termekek/[slug]/page.tsx
  </files>
  <action>
Polish product detail page:

1. Update breadcrumb styling
2. Add trust badges below add-to-cart
3. Improve gallery thumbnails with active state
4. Update tab styling for specs/documents
5. Add related products section with ProductCardEnhanced
6. Ensure consistent button styling

Changes:
- Add "Biztonságos fizetés" and "Gyors szállítás" badges
- Improve variant selector button styling
- Add stock indicator with green/red color
- Update certification badges styling
  </action>
  <verify>
    - Consistent with design system
    - All features work
    - Responsive
  </verify>
  <done>Product detail polished</done>
</task>

<task type="auto">
  <name>Task 3: Update category pages</name>
  <files>
    apps/web/src/app/[locale]/kategoriak/page.tsx
    apps/web/src/app/[locale]/kategoriak/[slug]/page.tsx
  </files>
  <action>
Update category pages:

1. Use CategoryGrid on main categories page
2. Update category detail to use ProductCardEnhanced
3. Improve breadcrumbs
4. Add category description section
  </action>
  <verify>
    - Consistent design
    - Grid layout correct
  </verify>
  <done>Category pages updated</done>
</task>

<task type="auto">
  <name>Task 4: Update ProductCard exports</name>
  <files>
    apps/web/src/components/product/index.ts
  </files>
  <action>
Update exports to include both old and new card:

```typescript
export { ProductCard } from './ProductCard';
export { ProductCardEnhanced } from './ProductCardEnhanced';
export { ProductCardSkeleton, ProductGridSkeleton } from './ProductCardSkeleton';
// ... other exports
```
  </action>
  <verify>
    - Exports updated
  </verify>
  <done>Exports updated</done>
</task>

</tasks>

<verification>
- Product listing uses enhanced cards
- Product detail polished
- Category pages consistent
- All responsive
</verification>

<success_criteria>
- Product pages match new design system
- Consistent user experience
</success_criteria>
