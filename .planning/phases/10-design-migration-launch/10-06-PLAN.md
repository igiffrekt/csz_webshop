---
phase: 10-design-migration-launch
plan: 06
type: implement
wave: 3
depends_on: [10-05]
files_modified:
  - apps/web/src/components/home/DealsSection.tsx
  - apps/web/src/components/home/PromoBanners.tsx
autonomous: true

must_haves:
  truths:
    - "Deals section with countdown"
    - "Promo banners"
  artifacts:
    - apps/web/src/components/home/DealsSection.tsx
    - apps/web/src/components/home/PromoBanners.tsx
---

<objective>
Create deals section and promotional banners for home page.

Purpose: Match reference design's deals and promotional areas.

Output: Deals section with featured products and promo banners.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create CountdownTimer component</name>
  <files>
    apps/web/src/components/ui/countdown-timer.tsx
  </files>
  <action>
Create countdown timer for deals:

```tsx
'use client';

import { useState, useEffect } from 'react';

interface CountdownTimerProps {
  endDate: Date;
  className?: string;
}

export function CountdownTimer({ endDate, className }: CountdownTimerProps) {
  const [timeLeft, setTimeLeft] = useState(calculateTimeLeft());

  function calculateTimeLeft() {
    const difference = endDate.getTime() - new Date().getTime();

    if (difference <= 0) {
      return { days: 0, hours: 0, minutes: 0, seconds: 0 };
    }

    return {
      days: Math.floor(difference / (1000 * 60 * 60 * 24)),
      hours: Math.floor((difference / (1000 * 60 * 60)) % 24),
      minutes: Math.floor((difference / 1000 / 60) % 60),
      seconds: Math.floor((difference / 1000) % 60),
    };
  }

  useEffect(() => {
    const timer = setInterval(() => {
      setTimeLeft(calculateTimeLeft());
    }, 1000);

    return () => clearInterval(timer);
  }, [endDate]);

  return (
    <div className={className}>
      <div className="flex items-center gap-2">
        <TimeBlock value={timeLeft.days} label="Nap" />
        <span className="text-xl font-bold text-secondary-400">:</span>
        <TimeBlock value={timeLeft.hours} label="Óra" />
        <span className="text-xl font-bold text-secondary-400">:</span>
        <TimeBlock value={timeLeft.minutes} label="Perc" />
        <span className="text-xl font-bold text-secondary-400">:</span>
        <TimeBlock value={timeLeft.seconds} label="Mp" />
      </div>
    </div>
  );
}

function TimeBlock({ value, label }: { value: number; label: string }) {
  return (
    <div className="bg-secondary-900 text-white rounded-lg p-2 min-w-[48px] text-center">
      <div className="text-lg font-bold leading-none">
        {String(value).padStart(2, '0')}
      </div>
      <div className="text-[10px] text-secondary-400 uppercase mt-1">
        {label}
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    - Countdown updates every second
    - Shows days, hours, minutes, seconds
  </verify>
  <done>CountdownTimer created</done>
</task>

<task type="auto">
  <name>Task 2: Create DealsSection component</name>
  <files>
    apps/web/src/components/home/DealsSection.tsx
  </files>
  <action>
Create deals of the day section:

```tsx
import Image from 'next/image';
import { Link } from '@/i18n/navigation';
import { Star, ArrowRight } from 'lucide-react';
import { formatPrice, cn } from '@/lib/utils';
import { CountdownTimer } from '@/components/ui/countdown-timer';
import type { Product } from '@csz/types';

interface DealsSectionProps {
  products: Product[];
}

export function DealsSection({ products }: DealsSectionProps) {
  // Get products on sale or first 2 products
  const dealProducts = products
    .filter(p => p.compareAtPrice && p.compareAtPrice > p.basePrice)
    .slice(0, 2);

  if (dealProducts.length === 0) {
    // Use first 2 products as fallback
    dealProducts.push(...products.slice(0, 2));
  }

  // End date for countdown (e.g., end of current day or week)
  const endOfDay = new Date();
  endOfDay.setHours(23, 59, 59, 999);

  return (
    <section className="section bg-white">
      <div className="container mx-auto px-4">
        {/* Section header */}
        <div className="flex flex-col md:flex-row md:items-end md:justify-between gap-4 mb-8">
          <div>
            <span className="inline-block px-3 py-1 bg-danger-100 text-danger-600 text-xs font-semibold rounded-full mb-2">
              Napi ajánlatok
            </span>
            <h2 className="text-3xl font-bold text-secondary-900">
              Mai Akciók
            </h2>
            <p className="text-secondary-600 mt-1">
              Korlátozott ideig érvényes ajánlatok
            </p>
          </div>

          <CountdownTimer endDate={endOfDay} />
        </div>

        {/* Deals grid */}
        <div className="grid md:grid-cols-2 gap-6">
          {dealProducts.map((product) => (
            <DealCard key={product.documentId} product={product} />
          ))}
        </div>
      </div>
    </section>
  );
}

function DealCard({ product }: { product: Product }) {
  const imageUrl = product.images?.[0]?.url
    ? `${process.env.NEXT_PUBLIC_STRAPI_URL || ''}${product.images[0].url}`
    : null;

  const isOnSale = product.compareAtPrice && product.compareAtPrice > product.basePrice;
  const discountPercent = isOnSale
    ? Math.round((1 - product.basePrice / product.compareAtPrice!) * 100)
    : 20; // Default discount for display

  return (
    <div className="relative bg-secondary-50 rounded-2xl p-6 flex flex-col md:flex-row gap-6 group">
      {/* Discount badge */}
      <div className="absolute top-4 left-4 z-10">
        <span className="inline-block px-3 py-1 bg-danger-500 text-white text-sm font-bold rounded-full">
          -{discountPercent}%
        </span>
      </div>

      {/* Image */}
      <div className="relative w-full md:w-1/3 aspect-square flex-shrink-0">
        <Link href={`/termekek/${product.slug}`}>
          {imageUrl ? (
            <Image
              src={imageUrl}
              alt={product.name}
              fill
              className="object-contain transition-transform duration-300 group-hover:scale-105"
            />
          ) : (
            <div className="w-full h-full bg-secondary-200 rounded-xl flex items-center justify-center">
              <span className="text-5xl">🧯</span>
            </div>
          )}
        </Link>
      </div>

      {/* Content */}
      <div className="flex-1 flex flex-col justify-center">
        {/* Category */}
        {product.category && (
          <span className="text-sm text-secondary-500">
            {product.category.name}
          </span>
        )}

        {/* Title */}
        <Link href={`/termekek/${product.slug}`}>
          <h3 className="text-xl font-bold text-secondary-900 mt-1 group-hover:text-primary-500 transition-colors">
            {product.name}
          </h3>
        </Link>

        {/* Rating */}
        <div className="flex items-center gap-1 mt-2">
          <div className="flex">
            {[1, 2, 3, 4, 5].map((star) => (
              <Star
                key={star}
                className={cn(
                  'h-4 w-4',
                  star <= 4 ? 'text-primary-500 fill-primary-500' : 'text-secondary-300'
                )}
              />
            ))}
          </div>
          <span className="text-sm text-secondary-500">5.0</span>
        </div>

        {/* Description */}
        <p className="text-secondary-600 text-sm mt-2 line-clamp-2">
          {product.shortDescription || product.description?.slice(0, 100)}
        </p>

        {/* Price */}
        <div className="flex items-center gap-3 mt-4">
          <span className="text-2xl font-bold text-secondary-900">
            {formatPrice(product.basePrice)}
          </span>
          {isOnSale && (
            <span className="text-lg text-secondary-400 line-through">
              {formatPrice(product.compareAtPrice!)}
            </span>
          )}
        </div>

        {/* CTA */}
        <Link
          href={`/termekek/${product.slug}`}
          className="inline-flex items-center gap-2 text-primary-500 font-medium mt-4 hover:text-primary-600 transition-colors"
        >
          Részletek
          <ArrowRight className="h-4 w-4" />
        </Link>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    - Countdown timer visible
    - Deal cards with discount badges
    - Product info displayed
  </verify>
  <done>DealsSection created</done>
</task>

<task type="auto">
  <name>Task 3: Create PromoBanners component</name>
  <files>
    apps/web/src/components/home/PromoBanners.tsx
  </files>
  <action>
Create side-by-side promotional banners:

```tsx
import { Link } from '@/i18n/navigation';
import { ArrowRight } from 'lucide-react';

interface PromoBanner {
  title: string;
  subtitle: string;
  discount?: string;
  ctaText: string;
  ctaLink: string;
  bgColor: string;
  textColor?: string;
}

const defaultBanners: PromoBanner[] = [
  {
    title: 'Poroltó készülékek',
    subtitle: 'Teljes kínálat egy helyen',
    discount: '20% kedvezmény',
    ctaText: 'Vásárlás most',
    ctaLink: '/kategoriak/porolto-keszulekek',
    bgColor: 'bg-gradient-to-r from-primary-500 to-primary-600',
    textColor: 'text-white',
  },
  {
    title: 'Tűzjelző rendszerek',
    subtitle: 'Professzionális megoldások',
    discount: '15% kedvezmény',
    ctaText: 'Vásárlás most',
    ctaLink: '/kategoriak/tuzjelzo-rendszerek',
    bgColor: 'bg-gradient-to-r from-secondary-800 to-secondary-900',
    textColor: 'text-white',
  },
];

interface PromoBannersProps {
  banners?: PromoBanner[];
}

export function PromoBanners({ banners = defaultBanners }: PromoBannersProps) {
  return (
    <section className="section-sm bg-white">
      <div className="container mx-auto px-4">
        <div className="grid md:grid-cols-2 gap-6">
          {banners.map((banner, index) => (
            <Link
              key={index}
              href={banner.ctaLink}
              className={`
                relative overflow-hidden rounded-2xl p-8 min-h-[200px]
                flex flex-col justify-center group
                ${banner.bgColor} ${banner.textColor || 'text-white'}
              `}
            >
              {/* Content */}
              <div className="relative z-10 max-w-xs">
                {banner.discount && (
                  <span className="inline-block px-3 py-1 bg-white/20 rounded-full text-sm font-medium mb-3">
                    {banner.discount}
                  </span>
                )}
                <h3 className="text-2xl font-bold mb-1">
                  {banner.title}
                </h3>
                <p className="text-white/80 mb-4">
                  {banner.subtitle}
                </p>
                <span className="inline-flex items-center gap-2 font-medium group-hover:gap-3 transition-all">
                  {banner.ctaText}
                  <ArrowRight className="h-5 w-5" />
                </span>
              </div>

              {/* Decorative circle */}
              <div className="absolute -right-16 -bottom-16 w-64 h-64 bg-white/10 rounded-full" />
              <div className="absolute -right-8 -bottom-8 w-48 h-48 bg-white/10 rounded-full" />
            </Link>
          ))}
        </div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
    - Two banners side by side
    - Gradient backgrounds
    - Hover effects
  </verify>
  <done>PromoBanners created</done>
</task>

<task type="auto">
  <name>Task 4: Export components</name>
  <files>
    apps/web/src/components/home/index.ts
  </files>
  <action>
Update exports.
  </action>
  <verify>
    - Exports added
  </verify>
  <done>Exports updated</done>
</task>

</tasks>

<verification>
- Countdown timer works
- DealsSection displays correctly
- PromoBanners responsive
</verification>

<success_criteria>
- Deals section matches reference design
- Promo banners are visually appealing
</success_criteria>
