---
phase: 10-design-migration-launch
plan: 07
type: implement
wave: 3
depends_on: [10-01]
files_modified:
  - apps/web/src/components/home/BlogSection.tsx
  - apps/web/src/components/layout/Footer.tsx
autonomous: true

must_haves:
  truths:
    - "Blog section with cards"
    - "Enhanced footer"
  artifacts:
    - apps/web/src/components/home/BlogSection.tsx
---

<objective>
Create blog section and enhance footer for home page.

Purpose: Match reference design's blog and footer areas.

Output: Blog section placeholder and multi-column footer.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create BlogSection component</name>
  <files>
    apps/web/src/components/home/BlogSection.tsx
  </files>
  <action>
Create blog/news section (placeholder until blog content exists):

```tsx
import Image from 'next/image';
import { Link } from '@/i18n/navigation';
import { Calendar, ArrowRight } from 'lucide-react';

interface BlogPost {
  title: string;
  excerpt: string;
  date: string;
  slug: string;
  image?: string;
}

// Placeholder posts until blog is implemented
const placeholderPosts: BlogPost[] = [
  {
    title: 'Tűzoltó készülék karbantartás: Mikor és hogyan?',
    excerpt: 'A tűzoltó készülékek rendszeres karbantartása kötelező és életmentő. Tudja meg, milyen gyakorisággal kell ellenőriztetni.',
    date: '2026-01-15',
    slug: 'tuzolto-keszulek-karbantartas',
  },
  {
    title: 'Tűzvédelmi szabályzat változások 2026-ban',
    excerpt: 'Az új év új szabályokat hoz a tűzvédelem területén. Összefoglaljuk a legfontosabb változásokat.',
    date: '2026-01-10',
    slug: 'tuzvedelmi-szabalyzat-2026',
  },
  {
    title: 'Hogyan válasszunk tűzjelző rendszert?',
    excerpt: 'A megfelelő tűzjelző rendszer kiválasztása kulcsfontosságú a biztonság szempontjából.',
    date: '2026-01-05',
    slug: 'tuzjelzo-valasztas',
  },
];

interface BlogSectionProps {
  posts?: BlogPost[];
}

export function BlogSection({ posts = placeholderPosts }: BlogSectionProps) {
  return (
    <section className="section bg-secondary-50">
      <div className="container mx-auto px-4">
        {/* Section header */}
        <div className="flex flex-col md:flex-row md:items-end md:justify-between gap-4 mb-8">
          <div>
            <span className="text-primary-500 font-medium text-sm uppercase tracking-wide">
              Hírek és Blog
            </span>
            <h2 className="text-3xl font-bold text-secondary-900 mt-1">
              Legfrissebb cikkeink
            </h2>
          </div>
          <Link
            href="/blog"
            className="inline-flex items-center gap-2 px-6 py-2 border-2 border-secondary-900 text-secondary-900 font-medium rounded-full hover:bg-secondary-900 hover:text-white transition-colors"
          >
            Összes cikk
            <ArrowRight className="h-4 w-4" />
          </Link>
        </div>

        {/* Blog grid */}
        <div className="grid md:grid-cols-3 gap-6">
          {posts.map((post) => (
            <BlogCard key={post.slug} post={post} />
          ))}
        </div>
      </div>
    </section>
  );
}

function BlogCard({ post }: { post: BlogPost }) {
  const formattedDate = new Date(post.date).toLocaleDateString('hu-HU', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });

  return (
    <article className="bg-white rounded-xl overflow-hidden shadow-sm hover:shadow-md transition-shadow group">
      {/* Image */}
      <div className="relative aspect-[16/10] bg-secondary-100">
        {post.image ? (
          <Image
            src={post.image}
            alt={post.title}
            fill
            className="object-cover"
          />
        ) : (
          <div className="w-full h-full flex items-center justify-center bg-gradient-to-br from-primary-100 to-primary-200">
            <span className="text-4xl">📰</span>
          </div>
        )}

        {/* Date badge */}
        <div className="absolute top-4 left-4">
          <span className="inline-flex items-center gap-1.5 px-3 py-1 bg-primary-500 text-white text-xs font-medium rounded-full">
            <Calendar className="h-3 w-3" />
            {formattedDate}
          </span>
        </div>
      </div>

      {/* Content */}
      <div className="p-5">
        <Link href={`/blog/${post.slug}`}>
          <h3 className="font-bold text-secondary-900 text-lg line-clamp-2 group-hover:text-primary-500 transition-colors">
            {post.title}
          </h3>
        </Link>
        <p className="text-secondary-600 text-sm mt-2 line-clamp-2">
          {post.excerpt}
        </p>
        <Link
          href={`/blog/${post.slug}`}
          className="inline-flex items-center gap-1 text-primary-500 font-medium text-sm mt-4 hover:gap-2 transition-all"
        >
          Tovább olvasom
          <ArrowRight className="h-4 w-4" />
        </Link>
      </div>
    </article>
  );
}
```
  </action>
  <verify>
    - Blog cards display correctly
    - Date formatting in Hungarian
    - Hover effects work
  </verify>
  <done>BlogSection created</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Footer component</name>
  <files>
    apps/web/src/components/layout/Footer.tsx
  </files>
  <action>
Redesign footer with multi-column layout:

```tsx
import { Link } from '@/i18n/navigation';
import { Facebook, Instagram, Linkedin, Youtube, Phone, Mail, MapPin } from 'lucide-react';

const productLinks = [
  { label: 'Tűzoltó készülékek', href: '/kategoriak/tuzolto-keszulekek' },
  { label: 'Tűzjelző rendszerek', href: '/kategoriak/tuzjelzo-rendszerek' },
  { label: 'Védőfelszerelések', href: '/kategoriak/vedofelszerelesek' },
  { label: 'Kiegészítők', href: '/kategoriak/kiegeszitok' },
  { label: 'Akciók', href: '/termekek?onSale=true' },
];

const companyLinks = [
  { label: 'Rólunk', href: '/rolunk' },
  { label: 'Kapcsolat', href: '/kapcsolat' },
  { label: 'GYIK', href: '/gyik' },
  { label: 'Blog', href: '/blog' },
  { label: 'Árajánlat kérés', href: '/ajanlatkeres' },
];

const supportLinks = [
  { label: 'Szállítási információk', href: '/szallitas' },
  { label: 'Visszaküldés', href: '/visszaterites' },
  { label: 'ÁSZF', href: '/aszf' },
  { label: 'Adatvédelem', href: '/adatvedelem' },
];

const socialLinks = [
  { icon: Facebook, href: 'https://facebook.com/csztuzvedelmi', label: 'Facebook' },
  { icon: Instagram, href: 'https://instagram.com/csztuzvedelmi', label: 'Instagram' },
  { icon: Linkedin, href: 'https://linkedin.com/company/csz-tuzvedelmi', label: 'LinkedIn' },
  { icon: Youtube, href: 'https://youtube.com/@csztuzvedelmi', label: 'YouTube' },
];

export function Footer() {
  return (
    <footer className="bg-secondary-900 text-white">
      {/* Main footer */}
      <div className="container mx-auto px-4 py-12">
        <div className="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-5 gap-8">
          {/* Brand column */}
          <div className="col-span-2 md:col-span-4 lg:col-span-1">
            <Link href="/" className="inline-block">
              <span className="text-2xl font-bold">
                CSZ <span className="text-primary-500">Tűzvédelem</span>
              </span>
            </Link>
            <p className="text-secondary-400 text-sm mt-4 max-w-xs">
              Professzionális tűzvédelmi megoldások üzletek, irodák és otthonok számára.
            </p>

            {/* Contact info */}
            <div className="mt-6 space-y-3">
              <a
                href="tel:+3612345678"
                className="flex items-center gap-3 text-secondary-400 hover:text-primary-500 transition-colors"
              >
                <Phone className="h-4 w-4" />
                <span className="text-sm">+36 1 234 5678</span>
              </a>
              <a
                href="mailto:info@csz-tuzvedelmi.hu"
                className="flex items-center gap-3 text-secondary-400 hover:text-primary-500 transition-colors"
              >
                <Mail className="h-4 w-4" />
                <span className="text-sm">info@csz-tuzvedelmi.hu</span>
              </a>
              <div className="flex items-start gap-3 text-secondary-400">
                <MapPin className="h-4 w-4 flex-shrink-0 mt-0.5" />
                <span className="text-sm">1234 Budapest, Példa utca 123.</span>
              </div>
            </div>

            {/* Social links */}
            <div className="flex items-center gap-3 mt-6">
              {socialLinks.map((social) => (
                <a
                  key={social.label}
                  href={social.href}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="w-10 h-10 bg-secondary-800 rounded-full flex items-center justify-center hover:bg-primary-500 transition-colors"
                  aria-label={social.label}
                >
                  <social.icon className="h-5 w-5" />
                </a>
              ))}
            </div>
          </div>

          {/* Product links */}
          <div>
            <h4 className="font-semibold text-white mb-4">Termékek</h4>
            <ul className="space-y-2">
              {productLinks.map((link) => (
                <li key={link.href}>
                  <Link
                    href={link.href}
                    className="text-secondary-400 text-sm hover:text-primary-500 transition-colors"
                  >
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </div>

          {/* Company links */}
          <div>
            <h4 className="font-semibold text-white mb-4">Cégünk</h4>
            <ul className="space-y-2">
              {companyLinks.map((link) => (
                <li key={link.href}>
                  <Link
                    href={link.href}
                    className="text-secondary-400 text-sm hover:text-primary-500 transition-colors"
                  >
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </div>

          {/* Support links */}
          <div>
            <h4 className="font-semibold text-white mb-4">Támogatás</h4>
            <ul className="space-y-2">
              {supportLinks.map((link) => (
                <li key={link.href}>
                  <Link
                    href={link.href}
                    className="text-secondary-400 text-sm hover:text-primary-500 transition-colors"
                  >
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </div>

          {/* Newsletter */}
          <div className="col-span-2 md:col-span-4 lg:col-span-1">
            <h4 className="font-semibold text-white mb-4">Hírlevél</h4>
            <p className="text-secondary-400 text-sm mb-4">
              Iratkozzon fel hírlevelünkre az akciókért és újdonságokért.
            </p>
            <form className="flex gap-2">
              <input
                type="email"
                placeholder="Email cím"
                className="flex-1 px-4 py-2 bg-secondary-800 border border-secondary-700 rounded-lg text-sm placeholder:text-secondary-500 focus:outline-none focus:ring-2 focus:ring-primary-500"
              />
              <button
                type="submit"
                className="px-4 py-2 bg-primary-500 text-white font-medium rounded-lg hover:bg-primary-600 transition-colors"
              >
                OK
              </button>
            </form>
          </div>
        </div>
      </div>

      {/* Bottom bar */}
      <div className="border-t border-secondary-800">
        <div className="container mx-auto px-4 py-4">
          <div className="flex flex-col md:flex-row items-center justify-between gap-4 text-sm text-secondary-400">
            <p>
              © {new Date().getFullYear()} CSZ Tűzvédelmi Kft. Minden jog fenntartva.
            </p>
            <div className="flex items-center gap-4">
              <Link href="/aszf" className="hover:text-primary-500 transition-colors">
                ÁSZF
              </Link>
              <Link href="/adatvedelem" className="hover:text-primary-500 transition-colors">
                Adatvédelem
              </Link>
            </div>
          </div>
        </div>
      </div>
    </footer>
  );
}
```
  </action>
  <verify>
    - Multi-column layout
    - All links present
    - Social icons
    - Newsletter form
    - Copyright
  </verify>
  <done>Footer redesigned</done>
</task>

<task type="auto">
  <name>Task 3: Export BlogSection</name>
  <files>
    apps/web/src/components/home/index.ts
  </files>
  <action>
Add export for BlogSection.
  </action>
  <verify>
    - Export added
  </verify>
  <done>Export added</done>
</task>

</tasks>

<verification>
- BlogSection displays placeholder posts
- Footer has all required sections
- Responsive layout
</verification>

<success_criteria>
- Blog and footer match reference design
- All links functional
</success_criteria>
